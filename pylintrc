[REPORTS]
msg-template="{msg_id}({symbol}) - {obj} Line #{line}: {msg}"

[MESSAGES CONTROL]
# Use this to remove uninteresting (or currently less important) warnings/errors
disable=invalid-name,
        too-few-public-methods,
        too-many-arguments
